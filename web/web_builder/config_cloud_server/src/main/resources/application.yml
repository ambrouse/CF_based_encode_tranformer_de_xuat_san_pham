server:
  port: 9000

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/ambrouse/config-cloud-server-fashion-web
          default-label: main
          search-paths: config
